::Have a nap [system] <863,600>
<div class="default-image-wrapper"><img src="pics/homeNap.jpg"></div>

You take a <<nobr>>

<<if $napHour > 0>>$napHour hour<<if $napHour > 1>>s<<endif>><<endif>>

<<if $napHour > 0 and $napMinute > 0>>and<<endif>>

<<if $napMinute > 0>>$napMinute minute<<if $napMinute > 1>>s<<endif>><<endif>>

<</nobr>> long nap. You feel a little more refreshed.

<<nobr>>

<<=$mainChar.takeNap({h:$napHour,m:$napMinute})>>

<</nobr>>

[[Wake up->Home bed]]
